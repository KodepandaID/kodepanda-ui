import { BorderSize, BorderSizeNum, BorderType, BorderPosition, RingSize } from "./border";
import { Color, Gradient, Palletes, Contrast, GradientPosition } from "./color";
import { FontSize, FontWeight } from "./font";
import { Rotate } from "./rotate";
import { RoundedPosition, RoundedSize } from "./rounded";
import { ShadowSize } from "./shadow";
import { Width, Height } from "./size";

export {
  BorderSize, BorderSizeNum, BorderType, BorderPosition, RingSize,
  Color, Gradient, Palletes, Contrast, GradientPosition,
  FontSize, FontWeight,
  Rotate,
  RoundedPosition, RoundedSize,
  ShadowSize,
  Width, Height
}